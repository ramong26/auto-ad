# 목표별 실행 계획

기준 문서: `README.md`  
MVP 목표 기간: 2026-08-10 ~ 2026-09-06

## 공통 원칙

- 첫 범위는 WordPress 1개, 블로그 콘셉트 1개, NAVER Search Trend, Telegram 승인, Obsidian, Codex, SQLite, WordPress REST API로 제한한다.
- 사용자 승인 없이 글을 발행하지 않는다.
- 출처가 부족하거나 고위험 주제(의료·금융·법률 등)는 초안을 만들거나 발행하지 않고 실패 사유를 남긴다.
- 비밀정보는 `.env`에만 두고 Vault·Git·로그에 기록하지 않는다.
- 실패는 다음 작업을 멈추지 않게 기록하고 재시도 가능한 상태로 남긴다.

## 목표 1 — 운영 범위와 콘텐츠 기준 확정

**목적:** 40~60대를 위한 생활·업무 디지털 문제 해결 블로그의 첫 운영 기준을 고정한다.

**할 일**

1. 블로그 이름, 도메인, 발행 빈도, 월 비용 한도를 결정한다.
2. `vault/personas/digital-life.md`에 독자·말투·해결할 문제·금지 주제를 작성한다.
3. evergreen 주제 30개와 글별 고유 가치(직접 확인, 비교, 체크리스트 등)를 정리한다.
4. 발행 전 필수 페이지 목록(소개, 연락처, 개인정보처리방침, 이용약관, 쿠키/광고, AI 사용, 출처·정정 정책)을 확정한다.

**완료 기준:** 어떤 독자의 어떤 문제를 해결하는지 한 문장으로 설명할 수 있고, 허용·금지 주제가 분리되어 있다.

## 목표 2 — MVP 기반 구축

**목적:** 자동화가 안전하게 실행될 최소 TypeScript 프로젝트를 만든다.

**할 일**

1. 비공개 저장소와 Node.js 24 LTS, pnpm, TypeScript 프로젝트를 준비한다.
2. `.gitignore`, `.env.example`, `AGENTS.md`, 실행·테스트 명령을 추가한다.
3. SQLite에 작업, 상태 전환, 중복 방지, 발행 idempotency를 구현한다.
4. 최소 단위 테스트로 주간 증가율, 0 나누기, 키워드 정규화, 점수, 상태 전환, slug, Telegram 소유자 검증을 확인한다.

**완료 기준:** `pnpm test`, `pnpm typecheck`가 통과하고, 같은 `keyword/date/blog` 조합은 중복 생성되지 않는다.

## 목표 3 — 주제 발굴과 승인 흐름

**목적:** NAVER 데이터에서 후보를 만들고 사용자가 선택한 주제만 다음 단계로 보낸다.

**할 일**

1. NAVER Search Trend API를 연결해 동일 14일 구간의 주간 증가율을 계산한다.
2. API 응답을 24시간 캐시하고 429·5xx·빈 결과를 작업 실패로 격리한다.
3. 콘셉트 적합성, 중복, 위험도를 반영해 상위 3개 후보를 고른다.
4. Telegram 허용 사용자에게 후보와 `글 작성/다른 주제/보류/오늘 건너뛰기` 버튼을 보낸다.
5. 선택 시 `vault/00-inbox`에 front matter 포함 Markdown 작업 파일을 만든다.

**완료 기준:** 실제 또는 모의 데이터로 후보 3개를 보내고, 버튼 승인 1회가 inbox 파일 1개를 만든다.

## 목표 4 — Codex 초안 작성과 검수

**목적:** 출처와 실용적 가치를 갖춘 초안만 검토 대기 상태로 만든다.

**할 일**

1. Vault 폴더와 inbox/draft 템플릿을 만든다.
2. Codex 예약 작업이 `inbox`와 `revision_requested`만 읽도록 지시한다.
3. persona와 공식 출처를 기반으로 WordPress용 HTML 본문, 제목, 요약, 메타 설명을 작성한다.
4. 중요 주장마다 출처 URL을 연결하고, 직접 확인·비교·체크리스트 중 하나 이상을 포함한다.
5. 출처 부족, 중복, 과장, 고위험 주장 발견 시 `90-failed`에 사유를 남긴다.
6. 성공한 초안은 `vault/20-drafts`로 저장하고 원본 상태를 `review`로 바꾼다.

**완료 기준:** inbox 1건이 출처 목록이 있는 draft 1건 또는 추적 가능한 실패 기록 1건으로 끝난다. 이 단계에서는 발행하지 않는다.

## 목표 5 — WordPress 발행 승인과 기록

**목적:** 최종 승인된 초안만 한 번 안전하게 발행한다.

**할 일**

1. HTTPS WordPress, 가벼운 테마, permalink, 카테고리, 최소 권한 Application Password를 설정한다.
2. REST API로 draft 생성, 미디어 업로드, slug 중복 조회를 검증한다.
3. Telegram에서 초안 링크와 `발행 승인/수정 요청/폐기`를 제공한다.
4. 승인 뒤에만 발행하고 URL·발행 시각·상태를 SQLite와 Vault에 기록한다.
5. timeout이면 slug를 조회해 중복 발행을 막고, 인증 실패면 즉시 중단해 사용자에게 알린다.

**완료 기준:** 승인 없는 발행은 불가능하고, 승인된 작업은 URL과 함께 `published` 상태로 기록된다.

## 목표 6 — MVP 전체 시나리오 검증

**목적:** 한 번의 작업이 후보 발굴부터 발행 기록까지 끊기지 않는지 확인한다.

**할 일**

1. 테스트 WordPress 또는 로컬 환경에서 전체 흐름을 실행한다.
2. 중복 승인, API 제한, Telegram 실패, Codex 실패, WordPress timeout/인증 실패를 각각 확인한다.
3. 실패 사유가 Telegram과 파일에 남고, 재시도가 중복 발행을 만들지 않는지 확인한다.
4. 운영 방법, 백업 방법, Application Password 폐기·재발급 방법을 README에 기록한다.

**완료 기준:** README의 MVP 완료 시나리오 12단계를 한 번 성공하며, 중복 발행 없이 실패 경로도 추적된다.

## 목표 7 — 콘텐츠 축적 및 AdSense 준비

**목적:** 자동화 완성 후 품질 있는 콘텐츠와 사이트 신뢰 요소를 쌓는다.

**할 일**

1. 주 3~5개 고품질 글을 발행하고 직접 테스트·스크린샷·공식 자료를 보강한다.
2. Search Console, Google Analytics, sitemap, robots.txt, ads.txt를 준비한다.
3. 정책 페이지와 모바일 표시, 내부 링크, 오래된 글의 업데이트 상태를 점검한다.
4. 글 수가 아니라 검색 노출·직접 방문·콘텐츠 품질을 기준으로 AdSense 신청 가능성을 판단한다.

**완료 기준:** 정책 페이지와 품질 점검이 끝나고, 콘텐츠·검색 지표를 근거로 신청 여부를 결정할 수 있다.

## 목표 8 — 성과 기반 확장

**목적:** 검증된 운영 방식만 확장한다.

**할 일**

1. 매주 발행 수, 인덱싱, 노출, 클릭, CTR, 평균 순위, 페이지뷰, 수익, 실패, 비용을 보고한다.
2. 성과가 낮은 글은 제목·구조·출처·검색 의도를 우선 개선한다.
3. 첫 사이트가 8~12주 운영, 검증 콘텐츠 30개 이상, 발행 성공률 98% 이상 등 README 기준을 충족할 때만 두 번째 WordPress를 검토한다.
4. Search Ads·Daum 수집·관리 화면·OpenAI API·Hermes는 실제 병목이 확인될 때만 추가한다.

**완료 기준:** 확장은 추측이 아닌 누적 데이터와 README의 확장 기준으로 결정된다.

## 권장 순서

1. 목표 1 완료
2. 목표 2 완료
3. 목표 3과 목표 4 완료
4. 목표 5 완료
5. 목표 6으로 MVP 통과
6. 목표 7 운영 후 목표 8 판단
